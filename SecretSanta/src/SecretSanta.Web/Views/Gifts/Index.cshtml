@model List<GiftViewModel>
@{
    ViewBag.Title = "Users";
}
@{int counter = 0;}

<section class="sm:grid grid-cols-3">
    @foreach(var giftViewModel in Model) {
        <section class="border-gray-500 border-2 p-6 m-5 rounded-lg">
            <a asp-controller="Gifts" asp-action="Edit" asp-route-id=@counter>@giftViewModel.Title</a>
            <form asp-controller="Gifts" asp-action="Delete" asp-route-id=@counter method="post">
                <button onclick="return confirm('You are about to delete @giftViewModel.Title. Are you sure you want to do this?');"><i class="fas fa-trash-alt"></i></button>
            </form>
            @{counter++;}
        </section>
    }
</section>

<section class="p-6 m-5">
    <a class="text-2xl leading-10" asp-controller="Gifts" asp-action="Create">Create</a>
</section>